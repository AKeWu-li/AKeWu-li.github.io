# --------------------------------------
# Hexo Butterfly Theme Configuration
# If you have any questions, please refer to the documentation
# Chinese: https://butterfly.js.org/
# English: https://butterfly.js.org/en/
# --------------------------------------

# --------------------------------------
# Navigation Settings
# --------------------------------------

nav:
  # Navigation bar logo image
  logo:
  display_title: true
  display_post_title: true
  # Whether to fix navigation bar
  fixed: false

menu:
# 格式：显示名称: 路径 || 图标
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  标签: /tags/ || fas fa-tags
  # 分类: /categories/ || fas fa-folder-open
  # 关于: /about/ || fas fa-heart
  # 清单:
  #   音乐: /music/ || fas fa-music
  #   电影: /movies/ || fas fa-video

# --------------------------------------
# Code Blocks Settings
# --------------------------------------

code_blocks:
  # Code block theme: darker / pale night / light / ocean / false
  theme: light
  macStyle: false
  # Code block height limit (unit: px)
  height_limit: false
  word_wrap: false

  # Toolbar
  copy: true
  language: true
  # true: shrink the code blocks | false: expand the code blocks | none: expand code blocks and hide the button
  shrink: false
  fullpage: true

# Social media links
# Formal:
#   icon: link || the description || color
# social:
  fab fa-github: https://github.com/AKeWu-li || GitHub || '#24292e'
  fab fa-bilibili: https://space.bilibili.com/176781832 || Bilibili || '#fb7299'

# --------------------------------------
# Image Settings
# --------------------------------------

favicon: /img/favicon.png

avatar:
  img: https://liiz-blog.oss-cn-shenzhen.aliyuncs.com/avatar.jpg
  effect: false

# Disable all banner images
disable_top_img: false

# If the banner of page not setting, it will show the default_top_img
default_top_img: "linear-gradient(to top, #dfe9f3 0%, white 100%)"

# The banner image of index page
index_img: https://liiz-blog.oss-cn-shenzhen.aliyuncs.com/background.jpg

# The banner image of archive page
archive_img:

# Note: tag page, not tags page
tag_img:

# The banner image of tag page, you can set the banner image for each tag
# Format:
#  - tag name: xxxxx
tag_per_img:

# Note: category page, not categories page
category_img:

# The banner image of category page, you can set the banner image for each category
# Format:
#  - category name: xxxxx
category_per_img:

# The background image of footer
footer_img: false

# Website Background
# Can set it to color, image URL or an array containing colors and/or image URLs
# If an array is provided, a random background will be selected from the array on each load
background: "linear-gradient(to top, #dfe9f3 0%, white 100%)"

cover:
  # Disable the cover or not
  index_enable: true
  aside_enable: true
  archives_enable: true
  # When cover is not set, the default cover is displayed
  default_cover: 
    # - xxx.jpg

# Replace Broken Images
error_img:
  flink: /img/friend_404.gif
  post_page: /img/404.jpg

# A simple 404 page
error_404:
  enable: false
  subtitle: 'Page Not Found'
  background: /img/error-page.png

post_meta:
  # Home Page
  page:
    # Choose: created / updated / both
    date_type: created
    # Choose: date / relative
    date_format: date
    categories: true
    tags: false
    label: true
  post:
    # Choose: left / center
    position: left
    # Choose: created / updated / both
    date_type: both
    # Choose: date / relative
    date_format: date
    categories: true
    tags: true
    label: true

# --------------------------------------
# Index page settings
# --------------------------------------

# The top_img settings of home page
# default: top img - full screen, site info - middle
# The position of site info, eg: 300px/300em/300rem/10%
index_site_info_top:
# The height of top_img, eg: 300px/300em/300rem
index_top_img_height:

# The subtitle on homepage
subtitle:
  enable: true
  # Typewriter Effect
  effect: true
  # Customize typed.js
  # https://github.com/mattboldt/typed.js/#customization
  typed_option:
  # Source - Call the third-party service API (Chinese only)
  # It will show the source first, then show the content of sub
  # Choose: false/1/2/3
  # false - disable the function
  # 1 - hitokoto.cn
  # 2 - https://api.aa1.cn/doc/yiyan.html
  # 3 - jinrishici.com
  source: false
  # If you close the typewriter effect, the subtitle will only show the first line of sub
  sub:
    - 此去泉台招旧部
    - 旌旗十万斩阎罗

# Article layout on the homepage
# 1: Cover on the left, info on the right
# 2: Cover on the right, info on the left
# 3: Cover and info alternate between left and right
# 4: Cover on top, info on the bottom
# 5: Info displayed on the cover
# 6: Masonry layout - Cover on top, info on the bottom
# 7: Masonry layout - Info displayed on the cover
index_layout: 3

# Display the article introduction on homepage
# 1: description
# 2: both (if the description exists, it will show description, or show the auto_excerpt)
# 3: auto_excerpt (default)
# false: do not show the article introduction
index_post_content:
  method: 3
  # If you set method to 2 or 3, the length need to config
  length: 500

# --------------------------------------
# Post Settings
# --------------------------------------

toc:
  post: true
  page: false
  number: true
  expand: false
  # Only for post
  style_simple: false
  scroll_percent: true

post_copyright:
  enable: true
  decode: false
  author_href:
  license: CC BY-NC-SA 4.0
  license_url: https://creativecommons.org/licenses/by-nc-sa/4.0/

# Sponsor/reward
reward:
  enable: false
  text:
  QR_code:
    # - img: /img/wechat.jpg
    #   link:
    #   text: wechat
    # - img: /img/alipay.jpg
    #   link:
    #   text: alipay

# Post edit
# Easily browse and edit blog source code online.
post_edit:
  enable: false
  # url: https://github.com/user-name/repo-name/edit/branch-name/subdirectory-name/
  # For example: https://github.com/jerryc127/butterfly.js.org/edit/main/source/
  url:

# Related Articles
related_post:
  enable: false
  # Number of posts displayed
  limit: 6
  # Choose: created / updated
  date_type: created

# Choose: 1 / 2 / false
# 1: The 'next post' will link to old post
# 2: The 'next post' will link to new post
# false: disable pagination
post_pagination: 1

# Displays outdated notice for a post
noticeOutdate:
  enable: false
  # Style: simple / flat
  style: flat
  # When will it be shown
  limit_day: 365
  # Position: top / bottom
  position: top
  message_prev: It has been
  message_next: days since the last update, the content of the article may be outdated.

# --------------------------------------
# Footer Settings
# --------------------------------------
footer:
  nav:
  owner:
    enable: false
    since: 2025
  # Copyright of theme and framework
  copyright:
    enable: false
    version: false
  custom_text:

# --------------------------------------
# Aside Settings
# --------------------------------------

aside:
  enable: true
  hide: true
  # Show the button to hide the aside in bottom right button
  button: true
  mobile: false
  # Position: left / right
  position: right
  display:
    archive: false
    tag: false
    category: false
  card_author:
    enable: false
    description:
    button:
      enable: false
      icon: fab fa-github
      text: GitHub
      link: https://github.com/AKeWu-li
  card_announcement:
    enable: false
    content: This is my Blog
  card_recent_post:
    enable: false
    # If set 0 will show all
    limit: 5
    # Sort: date / updated
    sort: date
    sort_order:
  card_newest_comments:
    enable: false
    sort_order:
    limit: 6
    # Unit: mins, save data to localStorage
    storage: 10
    avatar: true
  card_categories:
    enable: false
    # If set 0 will show all
    limit: 8
    # Choose: none / true / false
    expand: none
    sort_order:
  card_tags:
    enable: false
    # If set 0 will show all
    limit: 40
    color: false
    # Order of tags, random/name/length
    orderby: random
    # Sort of order. 1, asc for ascending; -1, desc for descending
    order: 1
    sort_order:
  card_archives:
    enable: false
    # Type: monthly / yearly
    type: monthly
    # Eg: YYYY年MM月
    format: MMMM YYYY
    # Sort of order. 1, asc for ascending; -1, desc for descending
    order: -1
    # If set 0 will show all
    limit: 8
    sort_order:
  card_post_series:
    enable: false
    # The title shows the series name
    series_title: false
    # Order by title or date
    orderBy: 'date'
    # Sort of order. 1, asc for ascending; -1, desc for descending
    order: -1
  card_webinfo:
    enable: false
    post_count: false
    last_push_date: false
    sort_order:
    # Time difference between publish date and now
    # Formal: Month/Day/Year Time or Year/Month/Day Time
    # Leave it empty if you don't enable this feature
    runtime_date:

# --------------------------------------
# Bottom right button
# --------------------------------------

# The distance between the bottom right button and the bottom (default unit: px)
rightside_bottom:

# Conversion between Traditional and Simplified Chinese
translate:
  enable: false
  # The text of a button
  default: 繁
  # the language of website (1 - Traditional Chinese/ 2 - Simplified Chinese）
  defaultEncoding: 2
  # Time delay
  translateDelay: 0
  # The text of the button when the language is Simplified Chinese
  msgToTraditionalChinese: '繁'
  # The text of the button when the language is Traditional Chinese
  msgToSimplifiedChinese: '簡'

# Read Mode
readmode: true

# Dark Mode
darkmode:
  enable: true
  # Toggle Button to switch dark/light mode
  button: true
  # Switch dark/light mode automatically
  # autoChangeMode: 1  Following System Settings, if the system doesn't support dark mode, it will switch dark mode between 6 pm to 6 am
  # autoChangeMode: 2  Switch dark mode between 6 pm to 6 am
  # autoChangeMode: false
  autoChangeMode: false
  # Set the light mode time. The value is between 0 and 24. If not set, the default value is 6 and 18
  start:
  end:

# Show scroll percent in scroll-to-top button
rightside_scroll_percent: false

# Don't modify the following settings unless you know how they work
# Choose: readmode,translate,darkmode,hideAside,toc,chat,comment
# Don't repeat the same value
rightside_item_order:
  enable: false
  # Default: readmode,translate,darkmode,hideAside
  hide:
  # Default: toc,chat,comment
  show:

# Animation for the bottom right config button
rightside_config_animation: true

# --------------------------------------
# Global Settings
# --------------------------------------

anchor:
  # When you scroll, the URL will update according to header id.
  auto_update: false
  # Click the headline to scroll and update the anchor
  click_to_scroll: false

photofigcaption: false

copy:
  enable: true
  # Add the copyright information after copied content
  copyright:
    enable: false
    limit_count: 100000

# Need to install the hexo-wordcount plugin
wordcount:
  enable: true
  # Display the word count of the article in post meta
  post_wordcount: false
  # Display the time to read the article in post meta
  min2read: false
  # Display the total word count of the website in aside's webinfo
  total_wordcount: true

# Busuanzi count for PV / UV in site
busuanzi:
  site_uv: false
  site_pv: false
  page_pv: false

# --------------------------------------
# Math
# --------------------------------------

# About the per_page
# if you set it to true, it will load mathjax/katex script in each page
# if you set it to false, it will load mathjax/katex script according to your setting (add the 'mathjax: true' or 'katex: true' in page's front-matter)
math:
  # Choose: mathjax, katex
  # Leave it empty if you don't need math
  use:
  per_page: true
  hide_scrollbar: false

  mathjax:
    # Enable the contextual menu
    enableMenu: true
    # Choose: all / ams / none, This controls whether equations are numbered and how
    tags: none

  katex:
    # Enable the copy KaTeX formula
    copy_tex: false

# --------------------------------------
# Search
# --------------------------------------

search:
  # Choose: algolia_search / local_search / docsearch
  # leave it empty if you don't need search
  use: local_search
  placeholder:

  # Algolia Search
  algolia_search:
    # Number of search results per page
    hitsPerPage: 6

  # Local Search
  local_search:
    # Preload the search data when the page loads.
    preload: false
    # Show top n results per article, show all results by setting to -1
    top_n_per_article: 1
    # Unescape html strings to the readable one.
    unescape: false
    # Enable pagination for search results
    pagination:
      enable: false
      # Number of search results per page
      hitsPerPage: 8
    CDN:

  # Docsearch
  # https://docsearch.algolia.com/
  docsearch:
    appId:
    apiKey:
    indexName:
    option:

# --------------------------------------
# Share System
# --------------------------------------

share:
  # Choose: sharejs / addtoany
  # Leave it empty if you don't need share
  use: 

  # Share.js
  # https://github.com/overtrue/share.js
  sharejs:
    sites: wechat

  # AddToAny
  # https://www.addtoany.com/
  addtoany:
    item: wechat

# --------------------------------------
# Comments System
# --------------------------------------

comments:
  # Up to two comments system, the first will be shown as default
  # Leave it empty if you don't need comments
  # Choose: Disqus/Disqusjs/Livere/Gitalk/Valine/Waline/Utterances/Facebook Comments/Twikoo/Giscus/Remark42/Artalk
  # Format of two comments system : Disqus,Waline
  use:
  # Display the comment name next to the button
  text: true
  # Lazyload: The comment system will be load when comment element enters the browser's viewport.
  # If you set it to true, the comment count will be invalid
  lazyload: true
  # Display comment count in post's top_img
  count: false
  # Display comment count in Home Page
  card_post_count: false

# Disqus
# https://disqus.com/
disqus:
  shortname:
  # For newest comments widget
  apikey:

# Alternative Disqus - Render comments with Disqus API
# https://github.com/SukkaW/DisqusJS
disqusjs:
  shortname:
  apikey:
  option:

# Livere
# https://www.livere.com/
livere:
  uid:

# Gitalk
# https://github.com/gitalk/gitalk
gitalk:
  client_id:
  client_secret:
  repo:
  owner:
  admin:
  option:

# Valine
# https://valine.js.org
valine:
  appId:
  appKey:
  avatar: monsterid
  # This configuration is suitable for domestic custom domain name users, overseas version will be automatically detected (no need to manually fill in)
  serverURLs:
  bg:
  # Use Valine visitor count as the page view count
  visitor: false
  option:

# Waline - A simple comment system with backend support fork from Valine
# https://waline.js.org/
waline:
  serverURL:
  bg:
  # Use Waline pageview count as the page view count
  pageview: false
  option:

# Utterances
# https://utteranc.es/
utterances:
  repo:
  # Issue Mapping: pathname/url/title/og:title
  issue_term: pathname
  # Theme: github-light/github-dark/github-dark-orange/icy-dark/dark-blue/photon-dark
  light_theme: github-light
  dark_theme: photon-dark
  js:
  option:

# Facebook Comments Plugin
# https://developers.facebook.com/docs/plugins/comments/
facebook_comments:
  app_id:
  # optional
  user_id:
  pageSize: 10
  # Choose: social / time / reverse_time
  order_by: social
  lang: en_US

# Twikoo
# https://github.com/imaegoo/twikoo
twikoo:
  envId:
  region:
  # Use Twikoo visitor count as the page view count
  visitor: false
  option:

# Giscus
# https://giscus.app/
giscus:
  repo:
  repo_id:
  category_id:
  light_theme: light
  dark_theme: dark
  js:
  option:

# Remark42
# https://remark42.com/docs/configuration/frontend/
remark42:
  host:
  siteId:
  option:

# Artalk
# https://artalk.js.org/guide/frontend/config.html
artalk:
  server:
  site:
  # Use Artalk visitor count as the page view count
  visitor: false
  option:

# --------------------------------------
# Chat Services
# --------------------------------------

chat:
  # Choose: chatra/tidio/crisp
  # Leave it empty if you don't need chat
  use:
  # Chat Button [recommend]
  # It will create a button in the bottom right corner of website, and hide the origin button
  rightside_button: false
  # The origin chat button is displayed when scrolling up, and the button is hidden when scrolling down
  button_hide_show: false

# https://chatra.io/
chatra:
  id:

# https://www.tidio.com/
tidio:
  public_key:

# https://crisp.chat/en/
crisp:
  website_id:

# --------------------------------------
# Analysis
# --------------------------------------

# https://tongji.baidu.com/web/welcome/login
baidu_analytics: 

# https://analytics.google.com/analytics/web/
google_analytics:

# https://www.cloudflare.com/zh-tw/web-analytics/
cloudflare_analytics:

# https://clarity.microsoft.com/
microsoft_clarity:

# https://umami.is/
umami_analytics:
  enable: false
  # For self-hosted setups, configure the hostname of the Umami instance
  serverURL:
  script_name: script.js
  website_id:
  option:
  UV_PV:
    site_uv: false
    site_pv: false
    page_pv: false
    # Umami Cloud (API key) / self-hosted Umami (token)
    token:

# https://www.googletagmanager.com/
google_tag_manager:
  tag_id:
  # optional
  domain:

# --------------------------------------
# Advertisement
# --------------------------------------

# Google Adsense
google_adsense:
  enable: false
  auto_ads: true
  js: https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js
  client:
  enable_page_level_ads: true

# Insert ads manually
# Leave it empty if you don't need ads
ad:
  # Insert ads in the index (every three posts)
  index:
  # Insert ads in aside
  aside:
  # Insert ads in the post (before pagination)
  post:

# --------------------------------------
# Verification
# --------------------------------------

site_verification:
  # - name: google-site-verification
  #   content: xxxxxx
  # - name: baidu-site-verification
  #   content: xxxxxxx

# --------------------------------------
# Beautify / Effect
# --------------------------------------

# Theme color for customize
# Notice: color value must in double quotes like "#000" or may cause error!

# theme_color:
#   enable: true
#   main: "#49B1F5"
#   paginator: "#00c4b6"
#   button_hover: "#FF7242"
#   text_selection: "#00c4b6"
#   link_color: "#99a9bf"
#   meta_color: "#858585"
#   hr_color: "#A4D8FA"
#   code_foreground: "#F47466"
#   code_background: "rgba(27, 31, 35, .05)"
#   toc_color: "#00c4b6"
#   blockquote_padding_color: "#49b1f5"
#   blockquote_background_color: "#49b1f5"
#   scrollbar_color: "#49b1f5"
#   meta_theme_color_light: "ffffff"
#   meta_theme_color_dark: "#0d0d0d"

# The user interface setting of category and tag page
# Choose: index - same as Homepage UI / default - same as archives UI
# leave it empty or index
category_ui:
tag_ui:

# Rounded corners for UI elements
rounded_corners_ui: true

# Stretches the lines so that each line has equal width
text_align_justify: true

# Add a mask to the header and footer
mask:
  header: true
  footer: true

# Loading Animation
preloader:
  enable: false
  # source
  # 1. fullpage-loading
  # 2. pace (progress bar)
  source: 1
  # pace theme (see https://codebyzach.github.io/pace/)
  pace_css_url: 

# Page Transition
enter_transitions: true

# Default display mode - light (default) / dark
display_mode: light

# Configuration for beautifying the content of the article
beautify:
  enable: true
  # Specify the field to beautify (site or post)
  field: post
  # Specify the icon to be used as a prefix for the title, such as '\f0c1'
  title_prefix_icon: 
  # Specify the color of the title prefix icon, such as '#F47466'
  title_prefix_icon_color: 

# Global font settings
# Don't modify the following settings unless you know how they work
font:
  global_font_size: 17px
  code_font_size: 17px
  font_family: LXGW WenKai Screen, Microsoft YaHei, sans-serif
  code_font_family: Fira Code, Consolas, Monaco, monospace

# Font settings for the site title and site subtitle
blog_title_font:
  font_link:
  font_family:

# The setting of divider icon
hr_icon:
  enable: true
  # The unicode value of Font Awesome icon, such as '\3423'
  icon:
  icon_top:

# Typewriter Effect
# https://github.com/disjukr/activate-power-mode
activate_power_mode:
  enable: false
  colorful: true
  shake: true
  mobile: false

# Background effects
# --------------------------------------

# canvas_ribbon
# See: https://github.com/hustcc/ribbon.js
canvas_ribbon:
  enable: false
  # The size of ribbon
  size: 150
  # The opacity of ribbon (0 ~ 1)
  alpha: 0.6
  zIndex: -1
  click_to_change: true
  mobile: false

# Fluttering Ribbon
canvas_fluttering_ribbon:
  enable: false
  mobile: false

# canvas_nest
# https://github.com/hustcc/canvas-nest.js
canvas_nest:
  enable: false
  # Color of lines, default: '0,0,0'; RGB values: (R,G,B).(note: use ',' to separate.)
  color: '0,0,255'
  # The opacity of line (0~1)
  opacity: 0.7
  # The z-index property of the background
  zIndex: -1
  # The number of lines
  count: 99
  mobile: false

# Mouse click effects: fireworks
fireworks:
  enable: false
  zIndex: 9999
  mobile: false

# Mouse click effects: Heart symbol
click_heart:
  enable: false
  mobile: false

# Mouse click effects: words
clickShowText:
  enable: false
  text:
    # - I
    # - LOVE
    # - YOU
  fontSize: 15px
  random: false
  mobile: false

# --------------------------------------
# Lightbox Settings
# --------------------------------------

# Choose: fancybox / medium_zoom
# https://github.com/francoischalifour/medium-zoom
# https://fancyapps.com/fancybox/
# Leave it empty if you don't need lightbox
lightbox: medium_zoom

# --------------------------------------
# Tag Plugins settings
# --------------------------------------

# Series
series:
  enable: false
  # Order by title or date
  orderBy: 'title'
  # Sort of order. 1, asc for ascending; -1, desc for descending
  order: 1
  number: true

# ABCJS - The ABC Music Notation Plugin
# https://github.com/paulrosen/abcjs
abcjs:
  enable: false
  per_page: true

# Mermaid
# https://github.com/mermaid-js/mermaid
mermaid:
  enable: false
  # Write Mermaid diagrams using code blocks
  code_write: false
  # built-in themes: default / forest / dark / neutral
  theme:
    light: default
    dark: dark

# chartjs
# see https://www.chartjs.org/docs/latest/
chartjs:
  enable: false
  # Do not modify unless you understand how they work.
  # The default settings are only used when the MD syntax is not specified.
  # General font color for the chart
  fontColor:
    light: 'rgba(0, 0, 0, 0.8)'
    dark: 'rgba(255, 255, 255, 0.8)'
  # General border color for the chart
  borderColor:
    light: 'rgba(0, 0, 0, 0.1)'
    dark: 'rgba(255, 255, 255, 0.2)'
  # Background color for scale labels on radar and polar area charts
  scale_ticks_backdropColor:
    light: 'transparent'
    dark: 'transparent'

# Note - Bootstrap Callout
note:
  # Note tag style values:
  #  - simple    bs-callout old alert style. Default.
  #  - modern    bs-callout new (v2-v3) alert style.
  #  - flat      flat callout style with background, like on Mozilla or StackOverflow.
  #  - disabled  disable all CSS styles import of note tag.
  style: flat
  icons: true
  border_radius: 3
  # Offset lighter of background in % for modern and flat styles (modern: -12 | 12; flat: -18 | 6).
  # Offset also applied to label tag variables. This option can work with disabled note tag.
  light_bg_offset: 0

# --------------------------------------
# Other Settings
# --------------------------------------

# https://github.com/MoOx/pjax
pjax:
  enable: true
  # Exclude the specified pages from pjax, such as '/music/'
  exclude:
    # - /xxxxxx/

# Inject the css and script (aplayer/meting)
aplayerInject:
  enable: false
  per_page: true

# Snackbar - Toast Notification
# https://github.com/polonel/SnackBar
# position: top-left / top-center / top-right / bottom-left / bottom-center / bottom-right
snackbar:
  enable: true
  position: bottom-left
  # The background color of Toast Notification in light mode and dark mode
  # bg_light: '#BDB76B'
  # bg_dark: '#BDB76B'

# Instant.page
# https://instant.page/
instantpage: true

# Lazyload
# https://github.com/verlok/vanilla-lazyload
lazyload:
  enable: true
  # Use browser's native lazyload instead of vanilla-lazyload
  native: false
  # Specify the field to use lazyload (site or post)
  field: site
  placeholder:
  blur: false

# PWA
# See https://github.com/JLHwung/hexo-offline
# ---------------
pwa:
  enable: false
  manifest:
  apple_touch_icon:
  favicon_32_32:
  favicon_16_16:
  mask_icon:

# Open graph meta tags
# https://hexo.io/docs/helpers#open-graph
Open_Graph_meta:
  enable: false
  option:
    # twitter_card:
    # twitter_image:
    # twitter_id:
    # twitter_site:
    # google_plus:
    # fb_admins:
    # fb_app_id:

# Structured Data
# https://developers.google.com/search/docs/guides/intro-structured-data
structured_data:
  enable: false
  # Alternate name for the site, used in structured data
  # Format: ['name1', 'name2']
  alternate_name:

# Add the vendor prefixes to ensure compatibility
css_prefix: true

# Inject
# Insert the code to head (before '</head>' tag) and the bottom (before '</body>' tag)
inject:
  head:
    # 1. 字体库
    - <link rel="stylesheet" href="https://npm.elemecdn.com/lxgw-wenkai-screen-webfont@1.7.0/lxgwwenkaigbscreen.css">
    - <link rel="stylesheet" href="https://unpkg.com/@firacode/cdn@latest/fired_code.css">
    - <link rel="stylesheet" href="/css/welcome.css">

    # 2. 样式部分 (整合版)
    - |
      <style>
        /* =========================================
           1. 底部透明化
           ========================================= */
        #footer, #footer-wrap { 
          background: transparent !important; 
          background-attachment: scroll !important; 
          box-shadow: none !important;
        }
        #footer::before, #footer-wrap::before { 
          background: transparent !important; 
          content: '' !important;
        }

        /* =========================================
           2. 修复时间轴图标 (保持你之前的黑色设置)
           ========================================= */
        .article-sort-item::before {
          content: '\f35a' !important;
          font-family: 'Font Awesome 6 Free' !important;
          font-weight: 900 !important;
          background: transparent !important;
          border: none !important;
          color: #000000 !important;
          font-size: 20px !important;
          width: auto !important;
          height: auto !important;
          left: -40px !important; 
          top: 2px !important;
          transition: all 0.3s ease-in-out;
        }

        .article-sort-title::before {
          content: '\f358' !important;
          font-family: 'Font Awesome 6 Free' !important;
          font-weight: 900 !important;
          background: transparent !important;
          border: none !important;
          color: #000000 !important;
          font-size: 24px !important;
          width: auto !important;
          height: auto !important;
          left: -10px !important;
          top: 20px !important;
          transition: all 0.3s ease-in-out;
        }

        .article-sort-item:hover::before,
        .article-sort-title:hover::before {
          transform: rotate(360deg);
          color: #00000f !important;
        }

        /* =========================================
           3. Snackbar 美化
           ========================================= */
        /* 针对最外层容器 */
        .snackbar-container {
          /* 1. 颜值：浅绿背景 */
          background: #dfe9f3 !important; 
          background-color: #dfe9f3!important;
          border-radius: 8px !important;
          
          /* 2. 布局：Flex 居中 */
          display: flex !important;
          justify-content: center !important;
          align-items: center !important;
          
          /* 3. 位置控制：不再依赖 JS 的 bottom 变化 */
          /* 强制固定在离底部 20px 的位置 */
          bottom: 0px !important; 
          left: 20px !important;
          
          /* 取消之前的位移，让它自然坐落 */
          transform: none !important; 
          
          /* 4. 过渡动画：只让透明度慢慢变 */
          transition: opacity 0.5s ease-in-out !important;
          
          /* 5. 尺寸 */
          min-width: 180px !important;
          border: none !important;
        }

        /* 🔥 核心逻辑：解决淡出问题 🔥 */
        /* 当 JS 把 opacity 设为 0 时 (准备隐藏) */
        .snackbar-container[style*="opacity: 0"] {
          /* 1. 保持 opacity: 0，配合上面的 transition 实现淡出 */
          opacity: 0 !important; 
          
          /* 2. 关键：不移动位置，让它原地淡出 */
          /* 3. 关键：禁止鼠标交互，防止点到透明的框 */
          pointer-events: none !important; 
        }

        /* 针对里面的文字 <p> */
        .snackbar-container p {
          font-family: 'LXGW WenKai Screen', sans-serif !important;
          font-size: 20px !important; 
          font-weight: bold !important;
          color: #00C2B5 !important;
          
          margin: 0 !important;
          padding: 6px 10px !important;
          text-align: center !important;
          line-height: 1.5 !important;
        }

        /* 图标部分：FontAwesome 对钩 */
        .snackbar-container p::before {
          content: '\f058'; 
          font-family: 'Font Awesome 6 Free' !important;
          font-weight: 900 !important;
          margin-right: 8px !important;
          font-size: 22px !important; 
        }
        
        /* 手机适配 */
        @media screen and (max-width: 768px) {
          .snackbar-container {
            left: 50% !important;
            transform: translateX(-50%) !important; /* 只做水平居中 */
          }
        }
        /* ==================================================
           4. 透明顶部栏配置 (精准避开首页版)
           ================================================== */
        
        /* 针对所有“非全屏”的顶部栏 (也就是内页) */
        /* 首页有 .full_page 类，所以这行代码绝不会碰到首页 */
        #page-header:not(.full_page) {
          background: transparent !important;
          background-image: none !important;
          height: 300px !important; /* 内页高度 */
          box-shadow: none !important;
          border: none !important;
          margin-bottom: 0 !important;
        }

        /* 去掉内页遮罩 */
        #page-header:not(.full_page)::before {
          background: transparent !important;
          box-shadow: none !important;
        }

        /* 5. 内页文字颜色修正 (深灰色) */
        /* 同样使用 :not(.full_page) 确保不改首页白字 */
        
        /* 站点标题 */
        #page-header:not(.full_page) #site-title,
        #page-header:not(.full_page) #site-subtitle,
        #page-header:not(.full_page) .post-meta,
        #page-header:not(.full_page) .page-title {
          color: #4c4948 !important;
          text-shadow: none !important;
        }
        
        /* =========================================
           6. 导航栏 (精准修复)
           ========================================= */
        
        /* 仅在非首页时，强制导航栏置顶且文字变黑 */
        
        /* 导航栏容器 */
        #page-header:not(.full_page) #nav {
          z-index: 9999 !important;
          background: transparent !important;
          box-shadow: none !important;
        }
        
        /* 博客名 (左上角) */
        #page-header:not(.full_page) #nav #blog_name a,
        #page-header:not(.full_page) #nav #site-name {
          color: #333333 !important;
          text-shadow: none !important;
          font-weight: bold !important;
        }

        /* 菜单链接 (右上角) */
        #page-header:not(.full_page) #nav .menus_item a {
          color: #333333 !important;
          text-shadow: none !important;
          font-weight: bold !important;
        }
        
        /* 搜索按钮 */
        #page-header:not(.full_page) #nav #search-button {
          color: #333333 !important;
        }
        
        /* 手机端汉堡菜单 */
        #page-header:not(.full_page) #nav .toggle-menu {
          color: #333333 !important;
        }
        
        /* 悬停颜色 */
        #page-header:not(.full_page) #nav .menus_item a:hover {
          color: #00C2B5 !important; 
        }
        /* ============================================================
           8. 终极色彩强制修复 (核弹版) - 专治颜色不生效
           ============================================================ */

        /* ------------------------------------------------------------
           场景 A: 首页 (Home) -> 背景是深色图 -> 文字必须是【白色】
           ------------------------------------------------------------ */
        /* 1. 博客标题 (左上角) */
        body.home #nav #blog_name .site-name,
        body.home #nav #site-name {
          color: #ffffff !important;
          text-shadow: 2px 2px 4px rgba(0,0,0,0.6) !important; /* 加重阴影，防背景撞色 */
        }
        
        /* 2. 菜单链接 (右上角) */
        body.home #nav .menus_item .site-page,
        body.home #nav .menus_item i {
          color: #ffffff !important;
          text-shadow: 1px 1px 2px rgba(0,0,0,0.5) !important;
        }

        /* 3. 搜索按钮 */
        body.home #nav #search-button {
           color: #ffffff !important;
           text-shadow: 1px 1px 2px rgba(0,0,0,0.5) !important;
        }

        /* ------------------------------------------------------------
           场景 B: 内页 (Not Home) -> 背景是透明/浅色 -> 文字必须是【深黑】
           ------------------------------------------------------------ */
        /* 1. 博客标题 (左上角) */
        body:not(.home) #nav #blog_name .site-name,
        body:not(.home) #nav #site-name {
          color: #333333 !important; /* 纯黑 */
          text-shadow: none !important; /* 去掉阴影 */
          font-weight: 900 !important; /* 加粗 */
        }

        /* 2. 菜单链接 (右上角) */
        body:not(.home) #nav .menus_item .site-page,
        body:not(.home) #nav .menus_item i {
          color: #333333 !important;
          text-shadow: none !important;
          font-weight: bold !important;
        }

        /* 3. 搜索按钮 */
        body:not(.home) #nav #search-button {
          color: #333333 !important;
        }
        
        /* 4. 归档页/标签页的大标题 (如“归档”) */
        body:not(.home) #page-header .page-title {
          color: #333333 !important;
          text-shadow: none !important;
        }

        /* 5. 文章具体页面：标题和元数据 */
        body:not(.home) #post-info .post-title {
          color: #000000 !important; /* 标题用纯黑 */
          text-shadow: none !important;
          font-weight: bold !important;
        }
        
        body:not(.home) #post-info #post-meta,
        body:not(.home) #post-info .post-meta-separator,
        body:not(.home) #post-info i {
          color: #555555 !important; /* 信息用深灰 */
          text-shadow: none !important;
        }
        
        /* 6. 鼠标悬停高亮 (统一用你的青绿色) */
        #nav .site-page:hover, 
        #nav #search-button:hover,
        #nav .site-name:hover {
          color: #00C2B5 !important;
        }
        /* =========================================
           9. 修复标签/分类/归档页居中 (保护文章侧边栏版)
           ========================================= */
        
        /* 核心逻辑：只有当页面 ID 是 #page 时，才执行去侧边栏和居中操作 */
        /* 文章页的 ID 是 #post，所以这下面的代码对文章页完全无效！ */

        /* 1. 隐藏侧边栏 (仅针对标签/归档页) */
        /* 使用兄弟选择器(~)：只有当侧边栏前面是 #page 时，才把它藏起来 */
        #page ~ #aside-content {
          display: none !important;
        }

        /* 2. 强制内容卡片居中 (仅针对标签/归档页) */
        .layout #page {
          /* 取消浮动，防止贴左 */
          float: none !important;
          
          /* 上下0，左右自动 = 居中 */
          margin: 0 auto !important;
          
          /* 稍微限制一下宽度，让它在屏幕中间更好看 */
          width: 90% !important; 
          max-width: 1200px !important; 
          
          /* 确保背景色和圆角 */
          padding: 40px !important;
        }
        /* =========================================
           10. 隐藏左上角标题 + 修复菜单位置
           ========================================= */
        
        /* 1. 隐藏博客标题 */
        #nav #blog-info {
          display: none !important;
        }

        /* 2. 🔥 核心修复：强制导航栏内容靠右对齐 🔥 */
        #nav {
          justify-content: flex-end !important;
        }
        /* =========================================
           11. 修改文章底部跳转卡片颜色 (终极毁灭版)
           ========================================= */
        
        /* 1. 强制设定背景色 */
        #pagination.pagination-post .cover {
          background: linear-gradient(to top, #dfe9f3 0%, #dfe9f3 100%) !important;
          opacity: 1 !important; /* 确保背景本身是不透明的 */
        }

        /* 2. ☢️ 地毯式清除所有可能的遮罩层 ☢️ */
        /* 只要是这下面的伪元素，统统隐藏！ */
        #pagination.pagination-post .cover::before,
        #pagination.pagination-post .cover::after,
        #pagination.pagination-post .prev-post > a::before,
        #pagination.pagination-post .next-post > a::before,
        #pagination.pagination-post .prev-post > a::after,
        #pagination.pagination-post .next-post > a::after {
          background: transparent !important;
          background-image: none !important;
          display: none !important; /* 直接不渲染，最彻底 */
          opacity: 0 !important;
          content: none !important; /* 取消内容 */
        }

        /* =========================================
           3. 文字颜色修改 (针对 info-item-1/2 结构)
           ========================================= */
        
        /* 选中“上一篇/下一篇”和“文章标题” */
        #pagination.pagination-post .info-item-1,
        #pagination.pagination-post .info-item-2 {
          /* 1. 字体颜色 */
          color: #00C2B5 !important; 
          
          /* 2. 必须去掉阴影，否则黑字配黑影会糊 */
          text-shadow: none !important; 
          
          /* 3. 加粗 */
          font-weight: bold !important;
        }

        /* =========================================
           4. 鼠标悬停变色
           ========================================= */
        /* 鼠标放上去时，文字变成主题色 */
        #pagination.pagination-post:hover .info-item-1,
        #pagination.pagination-post:hover .info-item-2 {
          color: #000000 !important;
        }
      </style>

    # 3. 引入 Typed.js
    - <script src="https://npm.elemecdn.com/typed.js@2.0.12/lib/typed.min.js"></script>

    # 4. 引入自定义 JS
    - <script src="/js/custom.js"></script>
  
  bottom:
    - <script src="https://liiz-blog.oss-cn-shenzhen.aliyuncs.com/js/live2d.min.js"></script>
    - |
      <script>
        // 设置一个定时器，等待 unpkg 的文件加载完毕
        var timer = setInterval(function() {
          // 只要 OML2D 还没加载好，就每 50ms 检查一次
          if (typeof OML2D !== 'undefined') {
            clearInterval(timer); // 加载好了！停止检查
            
            // 启动小人
            OML2D.loadOml2d({
              dockedPosition: "left", 
              menus: { disable: true },
              models: [
                {
                  path: 'https://liiz-blog.oss-cn-shenzhen.aliyuncs.com/live2d_models/yileina/LSS.model3.json',
                  position: [0, 150],
                  scale: 0.4,
                  stageStyle: {
                    height: 500
                  }
                }
              ]
            });
          }
        }, 50);
      </script>

# CDN Settings
# Don't modify the following settings unless you know how they work
CDN:
  # The CDN provider for internal and third-party scripts
  # Options for both: local/jsdelivr/unpkg/cdnjs/custom
  # Note: Dev version can only use 'local' for internal scripts
  # Note: When setting third-party scripts to 'local', you need to install hexo-butterfly-extjs
  internal_provider: local
  third_party_provider: cdnjs

  # Add version number to url, true or false
  version: true

  # Custom format
  # For example: https://cdn.staticfile.org/${cdnjs_name}/${version}/${min_cdnjs_file}
  custom_format:

  option:
    # abcjs_basic_js:
    # activate_power_mode:
    # algolia_js:
    # algolia_search:
    # aplayer_css:
    # aplayer_js:
    # artalk_css:
    # artalk_js:
    # blueimp_md5:
    # busuanzi:
    # canvas_fluttering_ribbon:
    # canvas_nest:
    # canvas_ribbon:
    # chartjs:
    # click_heart:
    # clickShowText:
    # disqusjs:
    # disqusjs_css:
    # docsearch_css:
    # docsearch_js:
    # egjs_infinitegrid:
    # fancybox:
    # fancybox_css:
    # fireworks: 
    # fontawesome:
    # gitalk:
    # gitalk_css:
    # giscus:
    # instantpage:
    # katex:
    # katex_copytex:
    # lazyload:
    # local_search:
    # main:
    # main_css:
    # mathjax:
    # medium_zoom:
    # mermaid:
    # meting_js:
    # prismjs_autoloader:
    # prismjs_js:
    # prismjs_lineNumber_js:
    # pjax:
    # sharejs:
    # sharejs_css:
    # snackbar:
    # snackbar_css:
    # translate:
    # twikoo:
    # typed:
    # utils:
    # valine:
    # waline_css:
    # waline_js:
    fontawesome: https://lib.baomitu.com/font-awesome/6.5.1/css/all.min.css